# Secret Santa Assignment (Rails)

This project generates Secret Santa assignments while ensuring:

- No employee is assigned to themselves
- No repeat from previous year (if provided)
- One-to-one mapping

---

## Setup

```bash
bundle install